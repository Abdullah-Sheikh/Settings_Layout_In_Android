<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item android:id="@+id/home_id"
       android:title="Home"
        android:icon="@drawable/home_icon"></item>

    <item android:id="@+id/chat_id"
        android:title="Chat"
        android:icon="@drawable/chat_icon"></item>

    <item android:id="@+id/bookings_id"
        android:title="Bookings"
        android:icon="@drawable/bokmark_icon"></item>

    <item android:id="@+id/profile_id"
        android:title="Profile"
        android:icon="@drawable/profile_icon"></item>

</menu>